kind = "web"
previewPath = "/"
title = "Casta"
version = "1.0.0"
id = "artifacts/casta"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24013

[services.development]
run = "pnpm --filter @workspace/casta run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/casta", "run", "build" ]
publicDir = "artifacts/casta/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24013"
BASE_PATH = "/"
